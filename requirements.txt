# WALLNUT Python Requirements
#
# The scripts in this repository (e.g., test_data/signal/generate_signals.py)
# use ONLY the Python 3 Standard Library.
#
# No external dependencies (such as numpy, scipy, or pandas) are needed. 
# You do not need to run `pip install -r requirements.txt`. 
#
# Simply ensure you are running Python 3.9 or higher:
#   python test_data/signal/generate_signals.py --help
